{{#if isAuthenticated}}
{{> user_nav }}
{{/if}}
<div class="row d-flex justify-content-around">
  <div class="col-2 user_profile_left">
    {{> user_profile_left }}
  </div>
  <div class="col-6">
    <div class="row">
      {{#each userLikedTweets}}
      {{> user_card_index}}
      {{/each}}
    </div>
  </div>
</div>
</div>